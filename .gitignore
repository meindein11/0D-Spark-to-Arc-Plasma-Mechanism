__pycache__/
*.pyc
.pytest_cache/

# Generated simulation outputs (regenerate via simulations/compare.py).
# The comparison plots (*.png) and summary table are committed so the
# results are visible without running anything; the raw per-timestep CSVs
# are left out since they're bulky and trivially regenerable.
railplug_0d/data/*.csv
